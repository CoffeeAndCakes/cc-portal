<h1>ALL EVENTS</h1>

<table>
 <tr>
   <th>Title</th>
   <th>Contents</th>
 </tr>

 <% @events.each do |event| %>
   <tr>
     <td><%= event.title %></td>
     <td><%= event.contents %>　さん</td>
     <td><%= link_to 'Show', event_path(event) %></td>
   </tr>
 <% end %>
</table>
